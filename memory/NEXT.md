# Next

Live backend mapping is in. Suggested follow-ups:

1. Chạy 2 tab ẩn danh: tạo phòng + join bằng mã 4 ký tự + ready + start + đánh bài.
2. Chat lobby / friends API khi user muốn.
3. Persist room settings (tên, public/private) nếu backend thêm endpoint.
4. Bloodlines engine khi có prompt luật.

Resume: đọc `memory/SESSION.md`.
